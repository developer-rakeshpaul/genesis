schema: http://localhost:4000/graphql
documents: ./graphql/**/*.gql
overwrite: true
generates:
  ./graphql/apollo-components.tsx:
    config:
      noNamespaces: true
    plugins:
      - typescript:
      - typescript-operations:
      - typescript-react-apollo:
